# ── Framework principal ────────────────────────────────────────────────────────
Django>=6.0,<7.0

# ── Base de datos PostgreSQL ───────────────────────────────────────────────────
psycopg2-binary>=2.9.12

# ── Servidor de producción ─────────────────────────────────────────────────────
gunicorn>=26.0

# ── Exportación a Excel ────────────────────────────────────────────────────────
openpyxl>=3.1.5

# ── Lectura rápida de Excel (Panel de Tareas WCS) ──────────────────────────────
# python-calamine (parser en Rust) es ~5x más rápido que openpyxl para leer
# archivos grandes — solo lee, no escribe, por eso openpyxl se mantiene para exportar.
python-calamine>=0.4.0

# ── Sirve archivos estáticos sin necesidad de nginx ────────────────────────────
whitenoise>=6.9.0

# ── Traducciones ───────────────────────────────────────────────────────────────
django-modeltranslation>=0.20.3

# ── Imágenes ───────────────────────────────────────────────────────────────────
pillow>=12.2.0
